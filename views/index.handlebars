<div class=container><h1 style="color:white; text-align: center">Eat Da Burger</h1></div>


<div class="row">
<div class="col-6">   
<h2 style="color:white">Burgers as yet undevoured. . .</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>
 </div>
   
   
 <div class="col-6">   
<h2 style="color:white">Burgers that have been DEVOURED.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>
 </div>
   
</div> 


<h2>Add a Burger</h2>

<form class="create-form">
  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="burger_name">
  <button type="submit" class="btn btn-danger">Add burger</button>
</form>



      